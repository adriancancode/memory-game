import "../styles/Card.css";

export default function Card({ title, description, imageUrl }) {
    return (
        <>
            <div>
                <img></img>
                <h2>{title}</h2>
                
            </div>
        </>
    );
}